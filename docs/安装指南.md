# 安装指南

NoteDoc 是一个开源的协作式 Wiki 和文档管理平台。本指南将帮助你快速部署 NoteDoc。

## 系统要求

### 最低配置

- **CPU**: 1 核心
- **内存**: 2GB RAM
- **存储**: 10GB 可用空间
- **操作系统**: Linux、macOS 或 Windows（支持 Docker）

### 推荐配置

- **CPU**: 2 核心或更多
- **内存**: 4GB RAM 或更多
- **存储**: 20GB 可用空间（根据使用情况调整）

### 依赖服务

- **PostgreSQL**: 16 或更高版本
- **Redis**: 7.2 或更高版本
- **Node.js**: 22.x（仅源码部署需要）

## 使用 Docker 部署（推荐）

Docker 是部署 NoteDoc 最简单快捷的方式。

### 前置条件

确保你的系统已安装：
- [Docker](https://docs.docker.com/get-docker/)（20.10 或更高版本）
- [Docker Compose](https://docs.docker.com/compose/install/)（2.0 或更高版本）

### 快速开始

1. **创建项目目录**

```bash
mkdir notedoc
cd notedoc
```

2. **下载 docker-compose.yml**

```bash
curl -O https://raw.githubusercontent.com/onlinedear/wiki/main/docker-compose.yml
```

或者手动创建 `docker-compose.yml` 文件：

```yaml
services:
  notedoc:
    image: notedoc/notedoc:latest
    depends_on:
      - db
      - redis
    environment:
      APP_URL: 'http://localhost:3000'
      APP_SECRET: 'REPLACE_WITH_LONG_SECRET'
      DATABASE_URL: 'postgresql://notedoc:STRONG_DB_PASSWORD@db:5432/notedoc?schema=public'
      REDIS_URL: 'redis://redis:6379'
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      - notedoc:/app/data/storage

  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: notedoc
      POSTGRES_USER: notedoc
      POSTGRES_PASSWORD: STRONG_DB_PASSWORD
    restart: unless-stopped
    volumes:
      - db_data:/var/lib/postgresql/data

  redis:
    image: redis:7.2-alpine
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  notedoc:
  db_data:
  redis_data:
```

3. **生成安全密钥**

```bash
# 生成 APP_SECRET（至少 32 字符）
openssl rand -hex 32

# 生成数据库密码
openssl rand -base64 24
```

4. **配置环境变量**

编辑 `docker-compose.yml` 文件，替换以下值：

- `APP_URL`: 你的域名或 IP 地址（例如：`https://notedoc.example.com`）
- `APP_SECRET`: 使用上一步生成的密钥
- `STRONG_DB_PASSWORD`: 使用上一步生成的数据库密码（需要在两处替换）

5. **启动 NoteDoc**

```bash
docker-compose up -d
```

6. **访问应用**

打开浏览器访问 `http://localhost:3000`（或你配置的域名）

首次访问时，系统会引导你创建管理员账户。

### Docker 常用命令

```bash
# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f notedoc

# 停止服务
docker-compose stop

# 启动服务
docker-compose start

# 重启服务
docker-compose restart

# 停止并删除容器
docker-compose down

# 更新到最新版本
docker-compose pull
docker-compose up -d
```

## 环境变量配置

### 必需配置

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `APP_URL` | 应用访问地址 | `https://notedoc.example.com` |
| `APP_SECRET` | 应用密钥（至少 32 字符） | 使用 `openssl rand -hex 32` 生成 |
| `DATABASE_URL` | PostgreSQL 连接字符串 | `postgresql://user:pass@host:5432/dbname` |
| `REDIS_URL` | Redis 连接字符串 | `redis://localhost:6379` |

### 可选配置

#### 端口设置

```bash
PORT=3000  # 应用监听端口（默认：3000）
```

#### 存储配置

```bash
# 存储驱动：local（本地）或 s3（对象存储）
STORAGE_DRIVER=local

# S3 兼容存储配置（当 STORAGE_DRIVER=s3 时）
AWS_S3_ACCESS_KEY_ID=your_access_key
AWS_S3_SECRET_ACCESS_KEY=your_secret_key
AWS_S3_REGION=us-east-1
AWS_S3_BUCKET=notedoc-bucket
AWS_S3_ENDPOINT=https://s3.amazonaws.com  # 可选，用于 S3 兼容服务
AWS_S3_FORCE_PATH_STYLE=false  # MinIO 等服务设置为 true
```

#### 文件上传限制

```bash
FILE_UPLOAD_SIZE_LIMIT=50mb  # 默认：50mb
```

#### 邮件服务配置

```bash
# 邮件驱动：smtp 或 postmark
MAIL_DRIVER=smtp
MAIL_FROM_ADDRESS=hello@example.com
MAIL_FROM_NAME=NoteDoc

# SMTP 配置
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your_username
SMTP_PASSWORD=your_password
SMTP_SECURE=false  # 使用 TLS
SMTP_IGNORETLS=false

# Postmark 配置（当 MAIL_DRIVER=postmark 时）
POSTMARK_TOKEN=your_postmark_token
```

#### JWT 配置

```bash
JWT_TOKEN_EXPIRES_IN=30d  # Token 过期时间（默认：30 天）
```

#### 其他配置

```bash
# 自定义 Draw.io 服务器
DRAWIO_URL=https://embed.diagrams.net

# 禁用遥测数据收集
DISABLE_TELEMETRY=false

# 生产环境调试模式
DEBUG_MODE=false
```

## 使用外部数据库

如果你已有 PostgreSQL 和 Redis 服务，可以使用简化的 Docker 配置：

```yaml
services:
  notedoc:
    image: notedoc/notedoc:latest
    environment:
      APP_URL: 'https://notedoc.example.com'
      APP_SECRET: 'your_secret_key_here'
      DATABASE_URL: 'postgresql://user:password@your-db-host:5432/notedoc'
      REDIS_URL: 'redis://your-redis-host:6379'
    ports:
      - "3000:3000"
    restart: unless-stopped
    volumes:
      - notedoc:/app/data/storage

volumes:
  notedoc:
```

### 数据库初始化

NoteDoc 会在首次启动时自动运行数据库迁移。确保：

1. PostgreSQL 数据库已创建
2. 数据库用户具有足够的权限（CREATE、ALTER、DROP 等）
3. 数据库版本为 PostgreSQL 16 或更高

## 反向代理配置

在生产环境中，建议使用反向代理（如 Nginx 或 Caddy）来处理 SSL/TLS 和域名访问。

### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name notedoc.example.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name notedoc.example.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    client_max_body_size 50M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Caddy 配置示例

```caddy
notedoc.example.com {
    reverse_proxy localhost:3000
    
    # 自动处理 SSL 证书
    encode gzip
    
    # 文件上传大小限制
    request_body {
        max_size 50MB
    }
}
```

## 云平台部署

### Vercel 部署

NoteDoc 支持一键部署到 Vercel：

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/onlinedear/wiki)

详细步骤请参考：
- [Vercel 部署指南](./Vercel部署指南.md)
- [快速部署到 Vercel](./快速部署到Vercel.md)

### Railway 部署

Railway 提供免费套餐，自动配置数据库：

1. 访问 [Railway](https://railway.app/)
2. 点击 "New Project"
3. 选择 "Deploy from GitHub repo"
4. 配置环境变量
5. 部署完成

详细步骤请参考 [一键部署方案](./一键部署方案.md#方案-3-railway快速测试)

### 其他云平台

- **Render**: [部署指南](./一键部署方案.md#方案-4-render托管服务)
- **DigitalOcean**: [部署指南](./一键部署方案.md#方案-5-digitalocean-app-platform)
- **AWS/GCP/Azure**: [完整部署指南](./NoteDoc完整部署指南.md)

## 宝塔面板部署

如果你使用宝塔面板管理服务器，可以通过 Docker 快速部署：

1. 在宝塔面板安装 Docker 管理器
2. 创建项目目录并上传 `docker-compose.yml`
3. 配置环境变量
4. 启动服务
5. 配置反向代理和 SSL 证书

详细的图文教程请参考 [宝塔面板部署指南](./宝塔面板部署指南.md)

## 源码部署

如果你需要从源码构建和部署 NoteDoc：

### 前置条件

- Node.js 22.x
- pnpm 10.4.0
- PostgreSQL 16+
- Redis 7.2+

### 部署步骤

1. **克隆仓库**

```bash
git clone https://github.com/onlinedear/wiki.git
cd wiki
```

2. **安装依赖**

```bash
npm install -g pnpm@10.4.0
pnpm install --frozen-lockfile
```

3. **配置环境变量**

```bash
cp .env.example .env
# 编辑 .env 文件，配置数据库和其他服务
```

4. **运行数据库迁移**

```bash
cd apps/server
pnpm migration:up
cd ../..
```

5. **构建应用**

```bash
pnpm build
```

6. **启动服务**

```bash
# 生产模式
pnpm start

# 或使用 PM2 管理进程
npm install -g pm2
pm2 start apps/server/dist/main.js --name notedoc-server
```

应用将在 `http://localhost:3000` 启动。

### 使用 PM2 管理

```bash
# 启动
pm2 start apps/server/dist/main.js --name notedoc

# 查看状态
pm2 status

# 查看日志
pm2 logs notedoc

# 重启
pm2 restart notedoc

# 停止
pm2 stop notedoc

# 开机自启
pm2 startup
pm2 save
```

## 数据备份

### 备份数据库

```bash
# 使用 Docker
docker-compose exec db pg_dump -U notedoc notedoc > backup_$(date +%Y%m%d).sql

# 直接使用 PostgreSQL
pg_dump -U notedoc -h localhost notedoc > backup_$(date +%Y%m%d).sql
```

### 备份文件存储

```bash
# 本地存储（Docker）
docker run --rm -v notedoc_notedoc:/data -v $(pwd):/backup alpine \
  tar czf /backup/storage_backup_$(date +%Y%m%d).tar.gz -C /data .

# 本地存储（源码部署）
tar czf storage_backup_$(date +%Y%m%d).tar.gz data/storage/
```

### 恢复数据

```bash
# 恢复数据库
docker-compose exec -T db psql -U notedoc notedoc < backup_20240101.sql

# 恢复文件存储
docker run --rm -v notedoc_notedoc:/data -v $(pwd):/backup alpine \
  tar xzf /backup/storage_backup_20240101.tar.gz -C /data
```

## 更新升级

### Docker 更新

```bash
# 拉取最新镜像
docker-compose pull

# 重启服务
docker-compose up -d

# 查看日志确认更新成功
docker-compose logs -f notedoc
```

### 源码更新

```bash
# 拉取最新代码
git pull origin main

# 安装依赖
pnpm install --frozen-lockfile

# 运行数据库迁移
cd apps/server
pnpm migration:up
cd ../..

# 重新构建
pnpm build

# 重启服务
pm2 restart notedoc
```

## 故障排查

### 应用无法启动

1. **检查日志**
```bash
docker-compose logs notedoc
```

2. **检查数据库连接**
```bash
docker-compose exec notedoc sh
# 在容器内测试数据库连接
```

3. **检查环境变量**
确保 `APP_SECRET`、`DATABASE_URL` 和 `REDIS_URL` 配置正确

### 数据库连接失败

1. 确认 PostgreSQL 服务正在运行
2. 检查数据库连接字符串格式
3. 验证数据库用户权限
4. 检查防火墙设置

### Redis 连接失败

1. 确认 Redis 服务正在运行
2. 检查 Redis 连接字符串
3. 验证 Redis 配置（密码、端口等）

### 文件上传失败

1. 检查 `FILE_UPLOAD_SIZE_LIMIT` 配置
2. 如使用 S3，验证 AWS 凭证和权限
3. 检查存储卷挂载是否正确
4. 确认反向代理的上传大小限制

### 性能问题

1. 增加服务器资源（CPU、内存）
2. 优化 PostgreSQL 配置
3. 启用 Redis 缓存
4. 使用 CDN 加速静态资源
5. 配置数据库连接池

## 安全建议

1. **使用强密码**
   - `APP_SECRET` 至少 32 字符
   - 数据库密码使用随机生成的强密码

2. **启用 HTTPS**
   - 使用 SSL/TLS 证书
   - 配置反向代理强制 HTTPS

3. **定期备份**
   - 设置自动备份计划
   - 测试备份恢复流程

4. **保持更新**
   - 定期更新 NoteDoc 到最新版本
   - 更新 PostgreSQL 和 Redis

5. **网络安全**
   - 使用防火墙限制数据库访问
   - 配置请求频率限制
   - 启用 MFA 多因素认证（企业版）

6. **监控日志**
   - 定期检查应用日志
   - 设置异常告警

## 获取帮助

如果遇到问题，可以通过以下方式获取帮助：

- **文档**: [https://notedoc.cn/docs](https://notedoc.cn/docs)
- **GitHub Issues**: [https://github.com/onlinedear/wiki/issues](https://github.com/onlinedear/wiki/issues)
- **讨论区**: [https://github.com/onlinedear/wiki/discussions](https://github.com/onlinedear/wiki/discussions)

## 下一步

安装完成后，你可以：

1. 创建管理员账户
2. 配置工作空间设置
3. 创建第一个文档库（Space）
4. 邀请团队成员
5. 探索高级功能：
   - [配置 SSO 单点登录](./SSO_快速开始.md)
   - [启用 MFA 多因素认证](./MFA_SSO_实现说明.md)
   - [使用 API 密钥](./API密钥使用完整指南.md)
   - [从 Confluence 导入内容](./Confluence在线导入功能说明.md)

祝你使用愉快！🎉
