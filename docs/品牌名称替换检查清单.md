# Docmost → NoteDoc 品牌名称替换检查清单

本文档列出了所有需要将"Docmost"替换为"NoteDoc"的文件位置。

## 📋 替换概览

- **总计需要修改的文件类型**：
  - 配置文件（package.json, docker-compose.yml等）
  - 前端代码（TypeScript/React）
  - HTML模板
  - 文档文件（Markdown）
  - 翻译文件（JSON）

---

## 1️⃣ 核心配置文件

### package.json
- **路径**: `package.json`
- **需要修改的内容**:
  - `"name": "docmost"` → `"name": "notedoc"`
  - `"homepage": "https://docmost.com"` → `"homepage": "https://notedoc.cn"` (如果有新域名)
  - `"@docmost/editor-ext"` → `"@notedoc/editor-ext"`

### docker-compose.yml
- **路径**: `docker-compose.yml`
- **需要修改的内容**:
  - 服务名: `docmost:` → `notedoc:`
  - 镜像: `image: docmost/docmost:latest` → `image: notedoc/notedoc:latest`
  - 数据库名: `POSTGRES_DB: docmost` → `POSTGRES_DB: notedoc`
  - 数据库用户: `POSTGRES_USER: docmost` → `POSTGRES_USER: notedoc`
  - 数据库URL: `postgresql://docmost:...@db:5432/docmost` → `postgresql://notedoc:...@db:5432/notedoc`
  - 卷名: `docmost:` → `notedoc:`
  - 容器名: `docmost_data` → `notedoc_data`

### Dockerfile
- **路径**: `Dockerfile`
- **需要修改的内容**:
  - `LABEL org.opencontainers.image.source="https://github.com/docmost/docmost"` → 更新为新仓库地址

---

## 2️⃣ 前端代码文件

### apps/client/index.html
- **路径**: `apps/client/index.html`
- **需要修改的内容**:
  - `<title>Docmost</title>` → `<title>NoteDoc</title>`
  - `<meta name="apple-mobile-web-app-title" content="Docmost" />` → `content="NoteDoc"`

### apps/client/public/manifest.json
- **路径**: `apps/client/public/manifest.json`
- **需要修改的内容**:
  - `"name": "Docmost"` → `"name": "NoteDoc"`
  - `"short_name": "Docmost"` → `"short_name": "NoteDoc"`

### apps/client/src/lib/config.ts
- **路径**: `apps/client/src/lib/config.ts`
- **需要修改的内容**:
  - `return "Docmost";` → `return "NoteDoc";` (第18行，默认应用名称)

### apps/client/src/hooks/use-app-name.ts
- **路径**: `apps/client/src/hooks/use-app-name.ts`
- **需要修改的内容**:
  - `return currentUser?.workspace?.name || 'Docmost';` → `|| 'NoteDoc';`

### apps/client/src/components/ui/dynamic-head.tsx
- **路径**: `apps/client/src/components/ui/dynamic-head.tsx`
- **需要修改的内容**:
  - `let globalAppName = 'Docmost';` → `let globalAppName = 'NoteDoc';` (第6行)
  - `const workspaceName = workspace?.name || 'Docmost';` → `|| 'NoteDoc';` (第18行)

### apps/client/src/components/layouts/global/app-header.tsx
- **路径**: `apps/client/src/components/layouts/global/app-header.tsx`
- **需要修改的内容**:
  - `{workspace?.name || "Docmost"}` → `|| "NoteDoc"`

### apps/client/src/components/ui/error-404.tsx
- **路径**: `apps/client/src/components/ui/error-404.tsx`
- **需要修改的内容**:
  - `<title>{t("404 page not found")} - Docmost</title>` → `- NoteDoc`

### apps/client/src/components/settings/app-version.tsx
- **路径**: `apps/client/src/components/settings/app-version.tsx`
- **需要修改的内容**:
  - GitHub链接: `https://github.com/docmost/docmost/releases` → 更新为新仓库地址（2处）

---

## 3️⃣ 翻译文件

### apps/client/public/locales/zh-CN/translation.json
- **路径**: `apps/client/public/locales/zh-CN/translation.json`
- **需要修改的内容**:
  - `"Create a new API key to access the Docmost API programmatically"` → `"...NoteDoc API..."`

### 其他语言翻译文件
需要检查所有语言的翻译文件：
- `apps/client/public/locales/*/translation.json`
- 搜索包含"Docmost"的字符串并替换

---

## 4️⃣ 文档文件

### README.md
- **路径**: `README.md`
- **需要修改的内容**:
  - 标题: `<h1><b>Docmost</b></h1>` → `<h1><b>NoteDoc</b></h1>`
  - 所有提到"Docmost"的地方（约20+处）
  - 文档链接: `https://docmost.com/docs` → 更新为新域名
  - GitHub链接: 更新仓库地址
  - Vercel部署按钮中的项目名: `project-name=docmost` → `project-name=notedoc`
  - 目录路径示例: `docmost/` → `notedoc/`
  - 部署路径: `/www/wwwroot/docmost` → `/www/wwwroot/notedoc`

### docs/Vercel部署指南.md
- **路径**: `docs/Vercel部署指南.md`
- **需要修改的内容**:
  - 标题和所有"Docmost"提及（约30+处）
  - GitHub仓库链接
  - 部署按钮链接

### docs/宝塔面板部署指南.md
- **路径**: `docs/宝塔面板部署指南.md`
- **需要修改的内容**:
  - 标题: `宝塔面板部署 Docmost 完整指南` → `...NoteDoc...`
  - 所有"Docmost"提及（约50+处）
  - 路径: `/www/wwwroot/docmost` → `/www/wwwroot/notedoc`
  - 容器名: `docmost`, `docmost_db`, `docmost_redis` → `notedoc_*`
  - 镜像名: `docmost/docmost` → `notedoc/notedoc`
  - 数据库配置中的所有"docmost"
  - 官方链接: `https://docmost.com/docs` → 更新
  - GitHub链接: `https://github.com/docmost/docmost` → 更新

### docs/快速部署到Vercel.md
- **路径**: `docs/快速部署到Vercel.md`
- **需要修改的内容**:
  - 部署按钮链接中的项目名
  - 所有"Docmost"提及

### docs/Docmost完整部署指南.md
- **路径**: `docs/Docmost完整部署指南.md`
- **需要修改的内容**:
  - 文件名本身需要重命名为: `NoteDoc完整部署指南.md`
  - 文件内所有"Docmost"提及

### 其他文档文件
需要检查并更新的文档（约60+个文件）：
- `docs/SSO_*.md` - SSO相关文档
- `docs/Confluence*.md` - Confluence集成文档
- `docs/API密钥*.md` - API密钥文档
- `docs/个人API密钥*.md` - 个人API密钥文档
- `docs/评论*.md` - 评论功能文档
- `docs/附件搜索*.md` - 附件搜索文档
- 所有其他docs目录下的.md文件

---

## 5️⃣ 示例和脚本文件

### examples/api-key-usage-examples.md
- **路径**: `examples/api-key-usage-examples.md`
- **需要修改的内容**:
  - 环境变量名: `DOCMOST_API_KEY`, `DOCMOST_WORKSPACE_ID`, `DOCMOST_API_URL`
  - 类名: `DocmostClient`
  - 所有"Docmost"提及

### 脚本文件
检查所有脚本文件中的"docmost"引用：
- `scripts/*.sh`
- 可能包含路径、容器名、服务名等

---

## 6️⃣ 后端代码（如果需要）

### 数据库迁移文件
- **路径**: `apps/server/src/database/migrations/*.ts`
- **检查内容**: 表名、注释中是否有"Docmost"

### 邮件模板
- **检查内容**: 邮件中的品牌名称
- 可能的位置: `apps/server/src/integrations/mail/`

---

## 🔍 搜索命令

使用以下命令查找所有"Docmost"引用：

```bash
# 搜索所有文件（不区分大小写）
grep -ri "docmost" . --exclude-dir={node_modules,.git,.nx,dist,build}

# 只搜索代码文件
grep -r "Docmost" . --include="*.ts" --include="*.tsx" --include="*.js" --include="*.jsx"

# 搜索配置文件
grep -r "docmost" . --include="*.json" --include="*.yml" --include="*.yaml"

# 搜索文档文件
grep -r "Docmost" docs/
```

---

## ✅ 替换建议

### 1. 使用查找替换工具
推荐使用IDE的全局查找替换功能：
- **查找**: `Docmost`
- **替换为**: `NoteDoc`
- **查找**: `docmost`
- **替换为**: `notedoc`

### 2. 注意事项
- ⚠️ **URL和域名**: 如果有新域名，需要同时更新
- ⚠️ **Docker镜像**: 需要重新构建并推送到新的镜像仓库
- ⚠️ **数据库**: 现有数据库名称不需要修改，只需修改新部署的配置
- ⚠️ **Git仓库**: 更新所有GitHub链接到新仓库地址
- ⚠️ **包名**: npm包名需要在package.json中更新

### 3. 测试清单
替换完成后需要测试：
- [ ] 应用能正常启动
- [ ] 页面标题显示正确
- [ ] Favicon显示正确
- [ ] Docker容器能正常构建和运行
- [ ] 数据库连接正常
- [ ] API功能正常
- [ ] 翻译显示正确
- [ ] 文档链接有效

---

## 📊 统计信息

根据搜索结果，大约需要修改：
- **代码文件**: 10+ 个
- **配置文件**: 5+ 个
- **文档文件**: 60+ 个
- **翻译文件**: 需要检查所有语言版本

**总计**: 约 80-100 个文件需要修改

---

## 🚀 执行步骤建议

1. **备份项目**: 先创建完整备份
2. **创建新分支**: `git checkout -b rename-to-notedoc`
3. **批量替换**: 使用IDE全局替换功能
4. **手动检查**: 检查关键文件（package.json, docker-compose.yml等）
5. **更新链接**: 更新所有外部链接（GitHub, 官网等）
6. **测试**: 本地测试所有功能
7. **提交**: 提交更改并推送

---

**最后更新**: 2025-11-23
