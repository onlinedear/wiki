---
creator: "${author}"
created: "${createTime}"
last_modified_by: "${author}"
last_modified: "${updateTime}"
version: "v1.0"
visibility: "internal"
---
# 飞书子文档导入 - 权限配置

## 当前问题

Wiki API 返回 404 错误，说明缺少必要的权限或使用了错误的 API。

## 解决方案

### 方案 1：配置 Wiki 权限（推荐）

访问飞书开放平台权限页面：
```
https://open.feishu.cn/app/你的AppID/auth
```

添加以下权限：
- ✅ `wiki:wiki:readonly` - 查看知识库

**步骤**：
1. 在权限列表中搜索 "wiki"
2. 勾选 `wiki:wiki:readonly`
3. 点击保存
4. 等待 5-10 分钟让权限生效
5. 重新测试导入

### 方案 2：使用文档 URL 中的 space_id

如果你的飞书文档 URL 包含 space_id，可以直接使用：

```
https://xxx.feishu.cn/wiki/space/xxxxx/docx/yyyyy
                              ^^^^^ space_id
                                          ^^^^^ document_token
```

我们可以修改代码来提取和使用 space_id。

### 方案 3：手动指定子文档（临时方案）

如果无法获取权限，可以：
1. 手动导入每个子文档
2. 在 NoteDoc 中手动建立父子关系

## 测试步骤

### 1. 配置权限后重启服务器

```bash
# 停止服务器（Ctrl+C）
pnpm dev
```

### 2. 重新导入

导入"Chat 聊天"文档

### 3. 查看日志

```bash
tail -f apps/server/logs/app.log | grep "📚"
```

**期望看到**：
```
📚 Found matching wiki node via search
📚 Node has children, fetching child list...
📚 Found 2 children via wiki API
```

**如果仍然失败**：
```
📚 Wiki search API failed: Request failed with status code 403
```
→ 权限未生效，等待更长时间或检查权限配置

## 飞书 API 文档

- Wiki API：https://open.feishu.cn/document/server-docs/docs/wiki-v2/space-node/list
- 权限说明：https://open.feishu.cn/document/home/introduction-to-scope-and-authorization/list-of-permissions-by-application-capability

## 下一步

1. **配置 wiki:wiki:readonly 权限**
2. **等待权限生效（5-10 分钟）**
3. **重启服务器**
4. **重新导入测试**

---

**创建时间**：2025-11-26  
**状态**：等待权限配置
